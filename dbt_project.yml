name: ae_assessment
version: "1.0.0"
profile: ae_assessment

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets: ["target", "dbt_packages"]

models:
  ae_assessment:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table

vars:
  grace_period_days: 28 # (4 weeks)
  # Last day of reliable history, August 2024 rows are likely a data issue
  analysis_end_date: "2024-07-31"
